<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="MyRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/circle_rating_bar_full</item>
        <item name="android:minHeight">11dip</item>
        <item name="android:maxHeight">11dip</item>
    </style>

    <style name="myTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="actionBarTheme">@style/myTheme.actionBar</item>
        <item name="colorPrimary">@color/main_tools_bg</item>
        <item name="actionBarSize">@dimen/tools_bar_height</item>
    </style>

    <!-- 图片选择Activity样式 -->
    <style name="myTheme.actionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
        <!-- 设置toolbar的menu字体的大小 -->
        <item name="android:textSize">16sp</item>
    </style>

</resources>
